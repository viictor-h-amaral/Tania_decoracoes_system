<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:userControl="clr-namespace:TaniaDecoracoes.WPFLibrary.UserControls"
                    xmlns:utils="clr-namespace:TaniaDecoracoes.WPFLibrary.Utils">

    <Style x:Key="FocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="GridButton.Static.Background" Color="#F8D7E6"/>
    <SolidColorBrush x:Key="GridButton.Static.Foreground" Color="#8A446C"/>

    <SolidColorBrush x:Key="GridButton.MouseOver.Background" Color="#F0B6D1"/>
    <SolidColorBrush x:Key="GridButton.MouseOver.Foreground" Color="#7A3A5C"/>

    <SolidColorBrush x:Key="GridButton.Pressed.Background" Color="#7A3A5C"/>
    <SolidColorBrush x:Key="GridButton.Pressed.Foreground" Color="White"/>

    <SolidColorBrush x:Key="GridButton.Disabled.Background" Color="#B5B5B5"/>
    <SolidColorBrush x:Key="GridButton.Disabled.Foreground" Color="#606060"/>

    <Style x:Key="GridButtonsStyle" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource GridButton.Static.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource GridButton.Static.Foreground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>

        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontFamily" Value="Quicksand"/>

        <Setter Property="Height" Value="50"/>

        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="border" Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        SnapsToDevicePixels="true"
                        CornerRadius="5">

                        <StackPanel Orientation="Horizontal" 
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">

                            <userControl:FontAwesomeIcon x:Name="Icon" 
                                                        IconCode="{TemplateBinding Tag}" 
                                                        Foreground="{TemplateBinding Foreground}"
                                                        VerticalAlignment="Center"
                                                        Margin="30, 3, 15, 3"
                                                        FontSize="25"/>


                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsDefaulted" Value="true">
                            <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{StaticResource GridButton.MouseOver.Background}"/>
                            <Setter Property="TextElement.Foreground" Value="{StaticResource GridButton.MouseOver.Foreground}"/>
                            <Setter Property="TextElement.FontWeight" Value="Bold"/>
                            <Setter Property="TextElement.Foreground" Value="{StaticResource GridButton.MouseOver.Foreground}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource GridButton.Pressed.Background}"/>
                            <Setter Property="TextElement.Foreground" TargetName="Icon" Value="{StaticResource GridButton.Pressed.Foreground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource GridButton.Disabled.Background}"/>
                            <Setter Property="TextElement.Foreground" TargetName="Icon" Value="{StaticResource GridButton.Disabled.Foreground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>