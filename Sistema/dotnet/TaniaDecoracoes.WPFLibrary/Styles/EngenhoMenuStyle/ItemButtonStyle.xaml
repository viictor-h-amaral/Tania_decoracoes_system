<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:userControl="clr-namespace:TaniaDecoracoes.WPFLibrary.UserControls"
                    xmlns:utils="clr-namespace:TaniaDecoracoes.WPFLibrary.Utils">

    <Style x:Key="FocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="ItemButton.Static.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="ItemButton.Static.Foreground" Color="#8A446C"/>

    <SolidColorBrush x:Key="ItemButton.MouseOver.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="ItemButton.MouseOver.Foreground" Color="#7A3A5C"/>

    <SolidColorBrush x:Key="ItemButton.Pressed.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="ItemButton.Pressed.Foreground" Color="Black"/>

    <SolidColorBrush x:Key="ItemButton.Disabled.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="ItemButton.Disabled.Foreground" Color="#606060"/>

    <Style x:Key="ItemButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource ItemButton.Static.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource ItemButton.Static.Foreground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>

        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Quicksand"/>

        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="border" Background="Transparent" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        SnapsToDevicePixels="true"
                        CornerRadius="0">

                        <StackPanel Orientation="Horizontal" 
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">

                            <ContentPresenter x:Name="contentPresenter" 
                                Focusable="False" 
                                HorizontalAlignment="Left"  
                                RecognizesAccessKey="True"
                                VerticalAlignment="Center"/>


                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsDefaulted" Value="true">
                            <Setter Property="Background" Value="{StaticResource ItemButton.Static.Background}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{StaticResource ItemButton.MouseOver.Background}"/>
                            <Setter Property="Foreground" Value="{StaticResource ItemButton.MouseOver.Foreground}"/>
                            <Setter Property="Foreground" Value="{StaticResource ItemButton.MouseOver.Foreground}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" Value="{StaticResource ItemButton.Pressed.Background}"/>
                            <Setter Property="Foreground" Value="{StaticResource ItemButton.Pressed.Foreground}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>